# Project notes for Claude

This is a multi-machine project (synced via git). When making changes that future-you should remember on either machine, write them into the project files (committed to git), not into local Claude memory.

## When working on lessons

- **Batch generation (1–5 problems):** the user invokes `/batch-lesson <slug1> <slug2> ...`. The full workflow lives in [`.claude/commands/batch-lesson.md`](.claude/commands/batch-lesson.md) — read that when the command fires.
- **Single-lesson or ad-hoc work:** read [`lessons/LESSON_DESIGN.md`](lessons/LESSON_DESIGN.md). It is the lean **index** pointing at section-specific files under `lessons/design/`.

**Loading rule (per PLAN-011):** Load `lessons/LESSON_DESIGN.md` by default. Load files under `lessons/design/` *only* when authoring the matching section. Do **not** preload them. The index's section table tells you what to load. For class names, use [`static/CLASSES.md`](static/CLASSES.md), never the full `static/lesson.css`.

**Quality gate (per PLAN-016, hardened by PLAN-019):** Before marking `lesson_status=generated`, a lesson must pass three checks: [`scripts/lint_lesson.py <slug>`](scripts/lint_lesson.py) (structure + the §6 code-line-resolves check), [`scripts/verify_animation.mjs <slug>`](scripts/verify_animation.mjs) (answer-correctness; also runs `lessons/<slug>/verify.py` if present), and [`scripts/render_check.mjs <slug>`](scripts/render_check.mjs) (headless: no JS error, every §6 step lights a code line, no horizontal overflow). The batch-lesson skill enforces these. For §1, use the inline canonical pattern in [`lessons/design/sec1_insight.md`](lessons/design/sec1_insight.md) — do not open full golden lessons. To survey the whole corpus run [`scripts/audit_lessons.py`](scripts/audit_lessons.py) (full lint + render, baseline-aware); to check planning-doc/lesson reconciliation run [`scripts/doctor.py`](scripts/doctor.py).

The legacy monolith lives at [`lessons/archive/LESSON_DESIGN_v2.md`](lessons/archive/LESSON_DESIGN_v2.md). Use the partitioned files under `lessons/design/` instead.

## When working on the broader project

See [AGENT_MD/](AGENT_MD/) for the project's spec, plan/report lifecycle, and authoring rules. The latest implementation plan is [PLAN-026](AGENT_MD/plan/plans/PLAN-026_dp_ladder.md) (DP Ladder — a 15-problem Dynamic-Programming on-ramp that ramps from a bare recurrence into gentle Mediums, for building DP fluency before the Medium/Hard DP set. It is a new disjoint dataset `data/dp_ladder.json` surfaced as a `General | DP Ladder` **view-switch inside the Warm-Up tab** (nav stays 6 tabs), gated by `scripts/check_dp_ladder.py` — which enforces **difficulty is non-decreasing across `order`** (Easy→Medium) on top of the usual disjointness/hover-card checks; landed 2026-07-26), building on [PLAN-025](AGENT_MD/plan/plans/PLAN-025_nav_declutter_and_roadmap_polish.md) (nav de-clutter + roadmap polish — dropped the `Practice`/`Learn` group labels and per-tab counts from the nav (counts recast per Practice page as **X of Y completed**), and made the roadmap SVG scale to fill the content column with a legend-only header and no horizontal scroll on large screens; presentation-only, no data/validator changes, landed 2026-07-15), building on [PLAN-024](AGENT_MD/plan/plans/PLAN-024_topic_roadmap_and_branding.md) (topic roadmap + crack_IT branding — the Problems tab now has a `Table | Roadmap` view switch rendering `data/roadmap.json`, a 20-node topic dependency DAG; **it stores edges and layout only — every count is derived from `problems.json` at render time, and `scripts/check_roadmap.py` fails if a node so much as has a `count` field**; also pinned the crack_IT logo + favicon, landed 2026-07-14), building on [PLAN-023](AGENT_MD/plan/plans/PLAN-023_tab_architecture_and_warmup.md) (tab architecture + Warm-Up — the dashboard is now **6 tabs in 2 groups**: *Practice* (Basics · Warm-Up · Problems) ‖ *Learn* (Foundations · Patterns · Algorithms); Prerequisites was renamed **Foundations** with a `#prerequisites` alias redirect; every Basics entry gained a `details` blurb + worked `example` behind an ⓘ hover card; and a new **Warm-Up** tab holds 30 easy LeetCode problems bridging Basics and the 211, gated by `scripts/check_warmup.py`, landed 2026-07-14), building on [PLAN-022](AGENT_MD/plan/plans/PLAN-022_basics_problems_tab.md) (Basics tab — 70 curated, language-agnostic learn-to-program drills across 7 sections, difficulty-ramped and de-duplicated from a user-supplied practice sheet; data gated by `scripts/check_basics.py`, landed 2026-07-14), [PLAN-021](AGENT_MD/plan/plans/PLAN-021_prerequisites_section.md) (Foundations tab — foundational data-structure / algorithm / concept knowledge with a topic-derived problem cascade and a few hero animations; data gated by `scripts/check_prerequisites.py`, landed 2026-06-05) and [PLAN-020](AGENT_MD/plan/plans/PLAN-020_mobile_friendly_responsive.md) (mobile-friendly responsive layout; render gate now also checks 390px). Note `AGENT_MD/spec.md` is a historical 2026-05-07 snapshot — this file plus `README.md` are the current source of truth.

**Problem catalogue (4 datasets, mutually disjoint slugs):** `data/basics.json` (70, language-agnostic drills) · `data/warmup.json` (30, easy LeetCode on-ramp) · `data/dp_ladder.json` (15, Easy→gentle-Medium DP on-ramp, the `DP Ladder` view inside the Warm-Up tab) · `data/problems.json` (211, the DSA set). Disjointness is validator-enforced, not a one-time audit — when adding to any of them, run the matching `scripts/check_*.py` (`check_dp_ladder.py` also enforces the ladder's difficulty stays non-decreasing).

**Adding a problem topic?** Give it a node in `data/roadmap.json` too — `scripts/check_roadmap.py` fails if any `problems.json` topic has no node, because an uncovered topic would silently disappear from the roadmap while its problems still sit in the table.
