<!-- AUTO-GENERATED by scripts/render_algorithms_sheet.py on 2026-06-03.
     Edit data/algorithms.json + re-run to update. -->

# Algorithms Reference

**Source:** `data/algorithms.json` · 121 entries · generated 2026-06-03

**Tier:** T1=primer · T2=core · T3=advanced  
**Relevance:** High / Med / Low  
**Kind:** Algo=algorithm · DS=data structure


## Foundational Algorithms

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Linear Search *(Sequential Search)* | Algo | T1 | High | `O(n)` | Scan elements one by one until the target is found; the floor against which every other se… |
| 2 | Binary Search *(Bisection)* | Algo | T1 | High | `O(log n)` | Halve the search range each step using a monotone predicate; foundation for 'binary search… |
| 3 | Hash Table *(Hash Map, Dictionary)* | DS | T1 | High | `O(1) average for insert/lookup/delete` | Maps keys to slots via a hash function; the workhorse behind nearly every O(n) interview s… |
| 4 | Bubble Sort *(Sinking Sort)* | Algo | T1 | Med | `O(n²) worst & average, O(n) best with early-exit` | Repeatedly swap adjacent out-of-order pairs; the canonical first-sort to teach (and almost… |
| 5 | Insertion Sort | Algo | T1 | Med | `O(n²) worst, O(n) on nearly-sorted input` | Grow a sorted prefix by inserting the next element into its correct position; the engine i… |
| 6 | Selection Sort | Algo | T1 | Med | `O(n²) all cases` | Repeatedly select the minimum of the remaining suffix and swap it into place; the minimum-… |
| 7 | MergeSort | Algo | T2 | High | `O(n log n) all cases` | Recursively split, sort halves, merge — the canonical D&C sort and the engine behind 'coun… |
| 8 | QuickSort *(Partition Exchange Sort)* | Algo | T2 | High | `O(n log n) average, O(n²) worst` | Pick a pivot, partition around it (Lomuto or Hoare), recurse on each side; the in-place D&… |
| 9 | HeapSort | Algo | T2 | High | `O(n log n) all cases; O(n) build-heap` | Build a max-heap in O(n), then repeatedly swap the root with the last element and sift dow… |
| 10 | Counting Sort | Algo | T2 | Med | `O(n + k) where k is the value range` | Tally each key's frequency, then write each key out 'count' times — the canonical linear-t… |
| 11 | Radix Sort | Algo | T2 | Med | `O(d × (n + b)) where d = #digits, b = base` | Apply counting-sort digit by digit (LSD or MSD); beats O(n log n) for fixed-width integers… |
| 12 | Bucket Sort | Algo | T3 | Med | `O(n) average for uniformly distributed keys; O(n²) worst` | Distribute keys into buckets by range, sort each bucket, concatenate; the workhorse behind… |
| 13 | Interpolation Search | Algo | T3 | Low | `O(log log n) on uniformly distributed sorted data; O(n) worst on skewed data` | Binary search where the probe position is estimated from the value distribution; faster th… |

## Graph Algorithms

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Breadth-First Search (BFS) | Algo | T1 | High | `O(V + E)` | Level-by-level exploration using a queue; the primer for every BFS/shortest-path-in-grid/s… |
| 2 | Depth-First Search (DFS) | Algo | T1 | High | `O(V + E)` | Recurse (or explicit stack) into one branch as deep as possible before backtracking; the e… |
| 3 | Topological Sort *(Topo Sort, Kahn's Algorithm)* | Algo | T1 | High | `O(V + E)` | Order a DAG so every edge points forward; two flavours — Kahn's (BFS over in-degree-0 node… |
| 4 | Dijkstra's Algorithm *(Dijkstra, SSSP)* | Algo | T1 | High | `O((V + E) log V) with binary heap; O(E + V log V) with Fibonacci heap` | Greedy shortest-path with a min-heap; relax outgoing edges of the closest unsettled vertex… |
| 5 | Bellman-Ford Algorithm | Algo | T2 | Med | `O(V × E)` | V−1 rounds of relaxing every edge; slower than Dijkstra but correct in the presence of neg… |
| 6 | A* Algorithm *(A-Star)* | Algo | T2 | Med | `O(E) with admissible heuristic and consistent priorities` | Heuristic-guided Dijkstra: priority key is g(n) + h(n) where h estimates remaining cost; t… |
| 7 | Kruskal's Algorithm *(Kruskal MST)* | Algo | T2 | High | `O(E log E) dominated by edge sort` | MST via sorted edges + Union-Find: add each edge if it joins two different components; cla… |
| 8 | Prim's Algorithm *(Prim MST, Jarník's Algorithm)* | Algo | T2 | High | `O((V + E) log V) with binary heap; O(E + V log V) with Fibonacci heap` | Grow the MST one vertex at a time from a seed by extracting the minimum-weight crossing ed… |
| 9 | Borůvka's Algorithm *(Boruvka MST, Sollin's Algorithm)* | Algo | T3 | Low | `O(E log V)` | MST via parallel component contraction: in each round, every component picks its cheapest … |
| 10 | Ford-Fulkerson Method | Algo | T2 | Low | `O(E × max_flow)` | Repeatedly find any augmenting path in the residual graph and push flow along it; the foun… |
| 11 | Edmonds-Karp Algorithm | Algo | T3 | Low | `O(V × E²)` | Ford-Fulkerson with BFS to choose the shortest augmenting path each round; first polynomia… |
| 12 | Dinic's Algorithm | Algo | T3 | Low | `O(V² × E); O(E × √V) on unit-capacity graphs (bipartite matching)` | Max-flow via alternating BFS level-graph + blocking-flow DFS phases — fast enough for comp… |
| 13 | Kosaraju's Algorithm *(Kosaraju–Sharir)* | Algo | T2 | Med | `O(V + E)` | Two DFS passes — finish-time order on the original graph, then DFS on the transpose; the m… |
| 14 | Tarjan's Algorithm | Algo | T3 | Med | `O(V + E)` | Single-pass SCC via DFS with lowlink values; harder to derive than Kosaraju's but only one… |
| 15 | Eulerian Path / Circuit *(Hierholzer's Algorithm)* | Algo | T3 | Low | `O(V + E)` | Walk that uses every edge exactly once; constructed in linear time by Hierholzer's algorit… |
| 16 | Hamiltonian Path / Cycle | Algo | T3 | Low | `O(2^V × V) via Held-Karp DP; NP-complete in general` | Visit every VERTEX exactly once (vs Eulerian's every edge); NP-complete in general, solvab… |

## Dynamic Programming

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Longest Increasing Subsequence (LIS) | Algo | T1 | High | `O(n²) basic DP; O(n log n) with patience-sort + BS` | Length of the longest strictly increasing subsequence; the canonical 1D DP-with-binary-sea… |
| 2 | Longest Common Subsequence (LCS) | Algo | T1 | High | `O(n × m)` | Length of the longest subsequence common to two sequences; the prototypical 2D DP and the … |
| 3 | Edit Distance (Levenshtein) *(Levenshtein Distance)* | Algo | T2 | High | `O(n × m)` | Minimum insertions, deletions, and substitutions to transform one string into another; the… |
| 4 | 0/1 Knapsack | Algo | T1 | High | `O(n × W) where W is capacity` | Pick a subset of items (each used 0 or 1 times) maximising value within weight W; the boun… |
| 5 | Subset Sum | Algo | T2 | High | `O(n × T) where T is target sum` | Decide whether a subset sums to a target; the 0/1-knapsack shape reduced to a boolean — th… |
| 6 | Rod Cutting | Algo | T2 | Med | `O(n²)` | Maximise revenue from cutting a length-n rod into integer-length pieces with a price table… |
| 7 | Matrix Chain Multiplication *(MCM)* | Algo | T2 | Med | `O(n³)` | Minimise the number of scalar multiplications needed to compute A1·A2·…·An; the prototypic… |
| 8 | Floyd-Warshall Algorithm *(APSP DP)* | Algo | T2 | Med | `O(V³)` | All-pairs shortest paths via a triple loop: dp[i][j] = min(dp[i][j], dp[i][k] + dp[k][j]);… |
| 9 | Traveling Salesman Problem (TSP) *(Held-Karp)* | Algo | T3 | Med | `O(2^V × V²) Held-Karp DP` | Visit every city exactly once at minimum cost; the canonical bitmask-DP capstone and an ex… |
| 10 | Bell Numbers and Catalan Numbers | Algo | T3 | Low | `O(n²) Bell triangle; O(n) Catalan from closed form` | Two combinatorial recurrences that show up across counting problems — Bell for set partiti… |

## String Algorithms

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Naive String Matching | Algo | T1 | Med | `O(n × m) worst` | Try every starting position in the text and compare character by character; the brute-forc… |
| 2 | Rabin-Karp Algorithm | Algo | T2 | Med | `O(n + m) average with a good hash; O(n × m) worst on adversarial inputs` | Rolling polynomial hash makes the inner comparison O(1) amortised — excellent for multi-pa… |
| 3 | Knuth-Morris-Pratt (KMP) Algorithm *(KMP, Failure Function)* | Algo | T2 | High | `O(n + m)` | Precompute the pattern's failure function so the text pointer never moves backward; the ca… |
| 4 | Z Algorithm | Algo | T2 | Med | `O(n)` | Build the Z-array in linear time; the simplest exact string matcher to derive from scratch… |
| 5 | Boyer-Moore Algorithm | Algo | T3 | Med | `O(n / m) best — sublinear; O(n × m) worst (Boyer-Moore-Horspool variant fixes some pathological cases)` | Scan the pattern right-to-left and use bad-character + good-suffix heuristics to skip larg… |
| 6 | Suffix Array | DS | T3 | Med | `O(n log n) construction (DC3 is O(n)); O(m log n) substring search` | Sorted array of all suffixes' start indices; the lighter, space-efficient cousin of the su… |
| 7 | Suffix Tree *(Ukkonen's Tree)* | DS | T3 | Low | `O(n) construction via Ukkonen's online algorithm` | Compressed trie of all suffixes; supports a vast family of substring queries in linear tim… |
| 8 | Aho-Corasick Algorithm | Algo | T3 | Low | `O(n + m + z) where n = text, m = total pattern length, z = #matches` | Multi-pattern string matching via a trie augmented with failure links (KMP generalised to … |

## Computational Geometry

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Sweep Line Algorithm | Algo | T2 | Med | `O(n log n) due to event-queue sort` | Move a vertical line left-to-right, processing discrete events (starts, ends, intersection… |
| 2 | Line Segment Intersection (Bentley-Ottmann) *(Bentley-Ottmann, Shamos-Hoey)* | Algo | T3 | Low | `O((n + k) log n) where k = number of intersections` | Find all intersections among n line segments by sweeping left-to-right and maintaining the… |
| 3 | Closest Pair of Points | Algo | T2 | Med | `O(n log n)` | Find the two nearest points in a set using divide-and-conquer; the classic proof that the … |
| 4 | Graham's Scan (Convex Hull) *(Graham Scan)* | Algo | T2 | Med | `O(n log n) dominated by the angular sort` | Sort by polar angle and sweep once, using a stack to discard right-turns; the O(n log n) a… |
| 5 | Jarvis March (Gift Wrapping) *(Gift Wrapping)* | Algo | T2 | Low | `O(n × h) where h = hull vertices — output-sensitive; O(n²) worst` | Start at the leftmost point and repeatedly pick the point that makes the smallest counter-… |
| 6 | Rotating Calipers | Algo | T3 | Low | `O(n) after O(n log n) hull construction` | Rotate a pair of supporting lines around a convex hull to solve diameter, width, and close… |
| 7 | Voronoi Diagram *(Fortune's Algorithm)* | Algo | T3 | Low | `O(n log n) via Fortune's sweep-line` | Partition the plane into regions nearest to each site; the dual of the Delaunay triangulat… |
| 8 | Delaunay Triangulation *(Bowyer-Watson)* | Algo | T3 | Low | `O(n log n) expected via randomised incremental` | The triangulation of a point set that maximises the minimum triangle angle; the dual of th… |

## Numerical and Optimization

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Newton-Raphson Method *(Newton's Method)* | Algo | T2 | Med | `O(k) iterations, each O(1) for simple functions` | Root-finding via iterated tangent-line extrapolation: x_{n+1} = x_n − f(x_n)/f'(x_n); quad… |
| 2 | Gradient Descent *(SGD, Mini-batch GD)* | Algo | T2 | High | `O(k × n) per epoch for batch; O(k) per step for SGD` | Iteratively step in the direction of steepest descent (−∇f); the universal first-order opt… |
| 3 | Simulated Annealing | Algo | T3 | Low | `O(k) iterations — problem-dependent schedule` | Probabilistically accept worse moves to escape local optima — the thermal annealing analog… |
| 4 | Genetic Algorithms *(Evolutionary Algorithms)* | Algo | T3 | Low | `O(k × P × F) where k=generations, P=population, F=fitness eval` | Evolve a population of candidate solutions via selection, crossover, and mutation — biolog… |
| 5 | Simplex Method (Linear Programming) *(Dantzig's Simplex)* | Algo | T3 | Low | `Exponential worst case; polynomial average case; Klee-Minty shows O(2^n) worst` | Walk the vertices of a convex polytope (feasible region) following improving edges until t… |
| 6 | Interior-Point Method (Barrier Method) *(Karmarkar's Algorithm, Barrier Method)* | Algo | T3 | Low | `O(n^{3.5} L) where L = input bit-length — polynomial` | Traverse the interior of the feasible polytope using barrier functions — polynomial time L… |
| 7 | Dynamic Time Warping (DTW) *(DTW)* | Algo | T2 | Low | `O(n × m) basic DP; O(n) with Sakoe-Chiba band constraint` | Align two temporal sequences by elastically warping the time axis to minimise total point-… |
| 8 | Fast Fourier Transform (FFT) *(FFT, Cooley-Tukey)* | Algo | T3 | Med | `O(n log n)` | Evaluate a degree-n polynomial at all nth roots of unity in O(n log n) by recursive halvin… |

## Data Structures

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Disjoint Set Union (Union-Find) *(Union-Find, DSU)* | DS | T1 | High | `O(α(n)) amortised per operation with union-by-rank + path compression` | Two arrays (parent + rank) supporting near-O(1) merge and find — the backbone of Kruskal's… |
| 2 | Trie (Prefix Tree) *(Prefix Tree, Digital Tree)* | DS | T1 | High | `O(m) insert/search/delete where m = key length` | Tree where each edge is a character and nodes mark shared prefixes — O(m) insert/search an… |
| 3 | Ternary Search Tree (TST) *(TST)* | DS | T3 | Low | `O(m) average; O(m log n) worst` | Combine BST ordering with trie prefix sharing — three children per node keep memory low wh… |
| 4 | Binary Search Tree (BST) *(BST)* | DS | T1 | High | `O(h) search/insert/delete where h = height; O(log n) balanced, O(n) degenerate` | Binary tree maintaining the BST invariant (left < node < right) — O(log n) when balanced; … |
| 5 | AVL Tree | DS | T2 | Med | `O(log n) search/insert/delete — strictly height-balanced (|h_L − h_R| ≤ 1)` | Self-balancing BST with a height invariant (|left_h − right_h| ≤ 1) enforced by single and… |
| 6 | Red-Black Tree | DS | T2 | Med | `O(log n) search/insert/delete` | Four colour invariants keep height ≤ 2 log n with O(1) amortised rotations per mutation — … |
| 7 | Splay Tree | DS | T3 | Low | `O(log n) amortised per operation; O(n) worst-case single op` | Self-adjusting BST that splays (moves) every accessed node to the root — O(log n) amortise… |
| 8 | Fenwick Tree (Binary Indexed Tree) *(BIT, Binary Indexed Tree)* | DS | T2 | High | `O(log n) point update and prefix-sum query` | Store partial sums in an array indexed by the lowest set bit — O(log n) prefix sums and po… |
| 9 | Segment Tree | DS | T2 | High | `O(log n) point update and range query; O(log n) range update with lazy propagation` | Divide the array recursively into halves; each node stores the aggregate of its range — an… |
| 10 | Bloom Filter | DS | T2 | Med | `O(k) insert and lookup where k = number of hash functions` | A bit-array plus k hash functions: set k bits on insert, query checks all k bits — probabi… |
| 11 | Skip List | DS | T3 | Low | `O(log n) expected search/insert/delete` | Layered linked lists with exponentially sparser express lanes — O(log n) expected ops with… |

## Advanced Graph

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Graph Coloring *(Greedy Coloring, Welsh-Powell)* | Algo | T3 | Low | `O(V + E) greedy; optimal coloring is NP-complete` | Assign colours to vertices so no two adjacent vertices share a colour; greedy approximatio… |
| 2 | Bipartite Matching (Hopcroft-Karp) *(Hungarian Algorithm, Hopcroft-Karp)* | Algo | T3 | Med | `O(E √V) Hopcroft-Karp; O(VE) augmenting-path (Ford-Fulkerson on bipartite graph)` | Find the maximum matching in a bipartite graph; Hopcroft-Karp achieves O(E √V) by augmenti… |
| 3 | PageRank Algorithm | Algo | T2 | Med | `O(k × (V + E)) for k power-iteration steps until convergence` | Model a random surfer who follows links with probability d and teleports with probability … |
| 4 | Spectral Graph Theory *(Spectral Clustering, Graph Laplacian)* | Algo | T3 | Low | `O(V³) for full eigendecomposition; O(k × V × E) for k leading eigenvectors via power iteration` | Use the eigenvalues and eigenvectors of the graph Laplacian to reveal community structure,… |
| 5 | Planarity Testing (Kuratowski / Boyer-Myrvold) *(Boyer-Myrvold, Left-Right Planarity Test)* | Algo | T3 | Low | `O(V + E) — linear time` | Determine if a graph can be drawn in the plane without edge crossings in O(V+E); rooted in… |

## Cryptography and Security

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | SHA Hash Family (SHA-1 / SHA-256 / SHA-3) *(SHA-256, SHA-3)* | Algo | T2 | Med | `O(n) where n = message length (block-by-block processing)` | Iterative block-by-block compression into a fixed-size digest; SHA-256's Merkle-Damgård co… |
| 2 | AES (Advanced Encryption Standard) *(Rijndael, AES-128)* | Algo | T2 | Med | `O(1) per block (10/12/14 rounds of byte operations on a 4×4 state matrix)` | Substitution-permutation network operating on a 4×4 byte state matrix through SubBytes, Sh… |
| 3 | DES / 3DES *(Data Encryption Standard, Triple DES)* | Algo | T2 | Low | `O(1) per 64-bit block (16 Feistel rounds)` | The 16-round Feistel cipher that defined symmetric encryption; historically important but … |
| 4 | Merkle Tree *(Hash Tree)* | DS | T2 | Med | `O(n) build; O(log n) proof generation and verification` | Binary tree of hashes where each parent = H(left∥right); changing any leaf invalidates O(l… |
| 5 | Diffie-Hellman Key Exchange *(DH, ECDH)* | Algo | T2 | Med | `O(log p) for modular exponentiation via fast-modular-exp` | Exchange a shared secret over an insecure channel using the computational hardness of the … |
| 6 | RSA Algorithm *(Rivest-Shamir-Adleman)* | Algo | T2 | High | `O(log e × log² n) encrypt/decrypt via fast-modular-exp on n-bit numbers` | Encrypt with public key e: C = M^e mod n; decrypt with private key d: M = C^d mod n — secu… |
| 7 | Elliptic Curve Cryptography (ECC) *(ECDSA, ECDH)* | Algo | T3 | Med | `O(log k) scalar multiplication on the curve via double-and-add` | Perform Diffie-Hellman and RSA on the group of points of an elliptic curve over a finite f… |

## Parallel and Distributed

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | MapReduce | Algo | T2 | High | `O(n/P) map + O(n log n / P) sort + O(n/P) reduce for P workers — network shuffle dominates in practice` | Decompose a computation into embarrassingly parallel Map steps and an aggregating Reduce s… |
| 2 | Work-Stealing Scheduler *(Work Stealing, Fork-Join)* | Algo | T3 | Low | `O(T₁/P + T∞) where T₁ = sequential work, T∞ = critical-path length, P = processors` | Each thread has a deque; idle threads steal tasks from the tail of others' deques — achiev… |
| 3 | Bitonic Sort | Algo | T3 | Low | `O(n log² n) comparisons on P = n/2 processors` | Merge a bitonic sequence (first increasing then decreasing) in log n parallel steps; the c… |
| 4 | Distributed Hash Table (DHT) *(Chord, Kademlia)* | DS | T2 | Med | `O(log n) hops to route a key to its responsible node` | Map each key to a node on a consistent-hashing ring so adding or removing a node only rema… |
| 5 | Paxos Consensus Algorithm *(Multi-Paxos, Classic Paxos)* | Algo | T3 | Med | `2 message round-trips (prepare + accept) per value; latency-bound, not compute-bound` | Reach agreement among n processes tolerating ⌊n/2⌋ failures via two phases — Prepare/Promi… |
| 6 | Raft Consensus Algorithm | Algo | T2 | High | `Same as Paxos: 2 RTTs per log entry in steady state` | Leader-based consensus protocol with randomised election timeouts and a strict log-matchin… |

## Miscellaneous

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Activity Selection Problem | Algo | T1 | High | `O(n log n) for sorting; O(n) greedy scan` | Select the maximum number of non-overlapping intervals by greedily picking the one that fi… |
| 2 | Huffman Encoding *(Huffman Coding, Optimal Prefix Code)* | Algo | T1 | Med | `O(n log n) using a min-heap` | Build a min-heap of symbol frequencies and repeatedly merge the two lowest into a new inte… |
| 3 | Quickselect *(Hoare's Selection Algorithm)* | Algo | T1 | High | `O(n) expected, O(n²) worst-case; O(n) worst with median-of-medians pivot` | Partition around a pivot like Quicksort but recurse only into the half containing the kth … |
| 4 | Reservoir Sampling | Algo | T2 | High | `O(n) one-pass over stream; O(k log(n/k)) with Algorithm Z for large streams` | Maintain a size-k reservoir and accept each incoming stream element with probability k/i —… |
| 5 | Karatsuba Algorithm *(Karatsuba Multiplication)* | Algo | T2 | Low | `O(n^{log₂ 3}) ≈ O(n^{1.585})` | Multiply two n-digit numbers with 3 (instead of 4) half-size multiplications via the ident… |
| 6 | Strassen's Matrix Multiplication *(Strassen Algorithm)* | Algo | T2 | Low | `O(n^{log₂ 7}) ≈ O(n^{2.807})` | Multiply two n×n matrices with 7 (instead of 8) n/2 × n/2 submatrix products via Strassen'… |
| 7 | Monte Carlo Method *(Monte Carlo Simulation)* | Algo | T2 | Med | `O(k) samples — error ∝ 1/√k (central limit theorem)` | Estimate a quantity by averaging over many random trials — the 1/√k convergence from the C… |
| 8 | Backtracking | Algo | T1 | High | `O(b^d) in the worst case where b = branching factor and d = solution depth; pruning can give enormous practical speedups` | Explore the solution space depth-first, pruning branches the moment a constraint is violat… |

## Domain-Specific

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Smith-Waterman Algorithm | Algo | T3 | Low | `O(n × m) — same as LCS/edit-distance DP` | Local sequence alignment via DP with a reset-to-zero clause — identifies the best matching… |
| 2 | Needleman-Wunsch Algorithm | Algo | T3 | Low | `O(n × m)` | Global sequence alignment — edit-distance DP with a biologically meaningful scoring matrix… |
| 3 | Canny Edge Detection *(Canny Detector)* | Algo | T2 | Low | `O(n × m) for an n×m image — single pass per stage` | Five-stage pipeline — blur, gradient, thin, threshold, hysteresis — that provably optimise… |
| 4 | RANSAC (Random Sample Consensus) *(Random Sample Consensus)* | Algo | T2 | Low | `O(k × n) where k = iterations; k = log(1-p)/log(1-(1-e)^s) for desired confidence p, outlier fraction e, sample size s` | Randomly sample the minimum model-fitting set, count inliers, repeat — the robust estimato… |
| 5 | B-Tree *(B+ Tree, B* Tree)* | DS | T2 | Med | `O(log_t n) search/insert/delete where t = branching factor (often 100–1000 on disk)` | Generalise the BST to nodes with up to t-1 keys and t children — wide, shallow trees minim… |
| 6 | Log-Structured Merge Tree (LSM Tree) *(LSM, Log-Structured Merge Tree)* | DS | T2 | Med | `O(1) amortised write (append-only); O(log n) read with Bloom-filter acceleration` | Accept writes into an in-memory buffer (MemTable); flush sorted runs (SSTables) to disk; m… |
| 7 | TCP Congestion Control (CUBIC / Reno) *(TCP Reno, TCP CUBIC)* | Algo | T2 | Low | `O(1) per ACK/NACK event — purely reactive` | Probe available bandwidth with additive increases then halve the window on packet loss (AI… |

## Advanced Theoretical

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Euclidean Algorithm (GCD) *(Euclid's Algorithm, GCD)* | Algo | T1 | High | `O(log min(a,b)) — Fibonacci inputs are worst case` | gcd(a, b) = gcd(b, a mod b) — the oldest non-trivial algorithm (~300 BCE), converges in O(… |
| 2 | Extended Euclidean Algorithm *(Extended GCD, Bezout Coefficients)* | Algo | T2 | Med | `O(log min(a,b)) — same as basic GCD` | Extend the Euclidean algorithm to return Bézout coefficients (x, y) with ax + by = gcd(a, … |
| 3 | Modular Multiplicative Inverse *(Modular Inverse, Fermat's Little Theorem Inverse)* | Algo | T2 | Med | `O(log p) via Fermat's little theorem (fast-modular-exp); O(log a) via extended GCD` | Find x such that ax ≡ 1 (mod m) — the divide operation of modular arithmetic, used in RSA,… |
| 4 | Sieve of Eratosthenes *(Prime Sieve, Linear Sieve)* | Algo | T1 | High | `O(n log log n) classic; O(n) linear sieve (each composite crossed out exactly once)` | Mark multiples of each prime starting from 2 — the O(n log log n) sieve that generates all… |
| 5 | Fast Modular Exponentiation *(Binary Exponentiation, Repeated Squaring)* | Algo | T1 | High | `O(log e) multiplications for base^e mod m` | Square the base and halve the exponent on each step — O(log e) multiplications for base^e … |
| 6 | Matrix Exponentiation *(Matrix Fast Power)* | Algo | T2 | Med | `O(k³ log n) for k×k matrices raised to power n` | Apply binary exponentiation to square matrices — encodes any linear recurrence in a k×k tr… |
| 7 | Karger's Minimum Cut Algorithm *(Karger-Stein)* | Algo | T3 | Low | `O(V² log³ V) for Karger-Stein with amplification; O(E × V²) naive Karger` | Randomly contract edges (merging endpoints) until two super-nodes remain — the resulting c… |
| 8 | Vertex Cover 2-Approximation | Algo | T3 | Low | `O(V + E) — one pass over edges` | Greedily pick uncovered edges and include both endpoints — the simplest 2-approximation al… |
| 9 | Set Cover Greedy Approximation | Algo | T3 | Low | `O(n × m) where n = universe size, m = number of sets — dominated by coverage tracking` | Greedily add the set that covers the most uncovered elements — achieves a H(n) ≈ ln n appr… |

## Emerging Areas

| # | Name | Kind | Tier | Rel | Time | Note |
|---|---|---|---|---|---|---|
| 1 | Q-Learning | Algo | T2 | Low | `O(S × A) per step where S = states, A = actions` | Tabular model-free reinforcement learning: update Q(s,a) ← Q(s,a) + α[r + γ max Q(s',a') -… |
| 2 | Deep Q-Networks (DQN) *(DQN, Deep RL)* | Algo | T3 | Low | `O(batch × network_layers) per gradient step` | Replace the Q-table with a neural network; stabilise training with an experience replay bu… |
| 3 | Grover's Algorithm | Algo | T3 | Low | `O(√N) oracle queries — quadratic speedup over classical O(N) search` | Quantum amplitude amplification finds a marked item in an unsorted database with O(√N) ora… |
| 4 | Shor's Algorithm *(Quantum Factoring)* | Algo | T3 | Low | `O((log N)³) quantum gate operations — polynomial in input size` | Reduce factoring to quantum period-finding via the quantum Fourier transform — polynomial … |
| 5 | Blockchain Consensus (Proof-of-Work / Proof-of-Stake) *(Nakamoto Consensus, Proof of Work)* | Algo | T2 | Low | `O(1) per block to verify (SHA256); mining difficulty is tuned so blocks arrive at target rate` | Chain blocks by hashing the previous block header into the new one; require a valid proof-… |
